// script that creates or uses the database my_db

import pymongo

# Connect to MongoDB
client = pymongo.MongoClient()

# Access the database
db = client["my_db"]

# Use the database
# ... your code here ...

# Close the connection
client.close()